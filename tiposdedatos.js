//Dato del tipo number

let edad = 33;
console.log(edad);

// Dato del tipo number con decimales

let precio = 150.50;
console.log(precio);

// Dato del tipo String con comillas dobles

let nombre = "Flor";
console.log(nombre);

// Dato del tipo String con comillas simple

let apellido = 'Esposito';
console.log(apellido);

// Dato booleano con valor true

let verdadero = true;
console.log(verdadero);

//Dato boolenao con valor false

let falso = false;
console.log(false);

// Dato del tipo NaN
let notANumber = NaN;
console.log(notANumber);

// Dato del tipo Null

let nulo = null;
console.log(nulo);

// Dato undifined

let sinValor = "SinValor";
console.log(sinValor);

// Dato del tipo objeto literal

let persona = {
    nombre: 'Flor',
    edad: 39
}
console.log(persona);

// Datos de tipo array

let paises = [ 'Argentina', 'Venezuela', 'Colombia'];
console.log(paises);



